# Published skills are byte-comparable payloads built on more than one OS.
# Normalize to LF in the repo and on checkout so a bundle is identical whether
# it was published from Windows or Linux, and matches the lab's build output.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
