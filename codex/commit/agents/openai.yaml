interface:
  display_name: Commit
  short_description: Chunk the working tree into clean commits
  default_prompt: Use $commit to split the current changes into clean commits.
policy:
  allow_implicit_invocation: false
