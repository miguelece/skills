interface:
  display_name: Post-Creation Audit
  short_description: Test, document, and clean up a session
  default_prompt: Use $post-creation-audit to close out this work session.
policy:
  allow_implicit_invocation: true
