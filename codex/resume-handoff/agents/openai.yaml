interface:
  display_name: Resume Handoff
  short_description: Resume work from a handoff document
  default_prompt: Use $resume-handoff to pick up from the last session's handoff.
policy:
  allow_implicit_invocation: true
